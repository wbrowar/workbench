<% let component = {
  title: "Button",
  description: "A new component.",
  background: "light",
  html: {
    demo: `${ include(paths.components.src + 'button/button.ejs', { c: {
      label: 'Button',
      url: '#',
    } }) }`,
    code:
`${ ('< %-').replace(' ','') } include(paths.components.src + 'button/button.ejs', { c: {
    label: 'Button',
    url: '#',
} }) ${ ('% >').replace(' ','') }`,
    options: [
      { "name": 'label', "required": false, "type": 'string', "description": `Text that appears on the button.` },
      { "name": 'target', "required": false, "type": 'string', "description": `Adds a target attribute to the button.` },
      { "name": 'url', "required": false, "type": 'string', "description": `URL that th user will be sent to upon clicking button. If a URL is not supplied, the button will switch from an 'a' element to a 'button' element.` }
    ]
  },
  twig: {
    code:
`{% import 'macros/component.twig' as component %}
{{ component.c('button', { label: 'Button', url: 'http://google.com' }) }}`,
    options: [
      { "name": 'label', "required": false, "type": 'string', "description": `Text that appears on the button.` },
      { "name": 'target', "required": false, "type": 'string', "description": `Adds a target attribute to the button.` },
      { "name": 'url', "required": false, "type": 'string', "description": `URL that th user will be sent to upon clicking button. If a URL is not supplied, the button will switch from an 'a' element to a 'button' element.` }
    ]
  }
} %>
<%- include(paths.starter.styleInventory + '_demo.ejs', { component: component, paths: paths, pkg: pkg, release: release }) %>