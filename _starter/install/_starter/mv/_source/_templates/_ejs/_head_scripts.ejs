        <!-- styles -->
        <link rel="stylesheet" href="<%- siteUrl %>css/app<%- filenameVersion %>.css" media="print" onload="this.media='all'">

        <!-- scripts -->
        <script type="text/javascript">
            var jsDevMode = <% if (pkg.projectType === 'craft3') { %>{{ craft.app.config.general.devMode ? 'true' : 'false' }}<% } else { %><% if (release) { %>false<% } else { %>true<% } %><% } %>;

            // remove no-js class for when Javascript is enabled
            document.documentElement.className = document.documentElement.className.replace(/\bno-js\b/g, '') + ' js ';
        </script>