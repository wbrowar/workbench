{
  "scripts": {
    "postbuild": "node ./_starter/postbuild.js --marketovars",
    "prod": "npm run prebuild && vue-cli-service build && npm run postbuild"
  },
  "dependencies": {
    "loadjs": "^4.2.0"
  }
},