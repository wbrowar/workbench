{
  "scripts": {
    "postbuild": "node ./_wb/postbuild.js --marketovars",
    "prod": "VUE_APP_DEV_MODE=false VUE_APP_DOCS_URL=false npm run prebuild && vue-cli-service build && npm run postbuild"
  },
  "dependencies": {
    "loadjs": "^4.2.0",
    "esm": "^3.2.25"
  }
}